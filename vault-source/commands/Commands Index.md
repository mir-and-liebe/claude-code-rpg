# Commands Index

> Source: `~/.claude/commands/` — 39 commands
> Last synced: 2026-03-26

| Command | Description |
|---------|-------------|
| `/api-route` | Scaffold a Next.js App Router API route with Supabase |
| `/aside` | Answer a quick side question without interrupting or losing context from the current task. Resume work automatically aft |
| `/build-fix` | Incrementally fix build and type errors with minimal, safe changes |
| `/checkpoint` | Create or verify a checkpoint in your workflow |
| `/context-budget` | Analyze context window usage across agents, skills, MCP servers, and rules to find optimization opportunities. Helps red |
| `/delegate` | Delegate a junior-level task to Codex CLI. Claude reviews the result. Use for docs, test data, scaffolding, recon, and m |
| `/devfleet` | Orchestrate parallel Claude Code agents via Claude DevFleet — plan projects from natural language, dispatch agents in  |
| `/docs` | Look up current documentation for a library or topic via Context7. |
| `/end-session` | Run the end-session protocol to wrap up and persist state before session ends |
| `/evolve` | Analyze instincts and suggest or generate evolved structures |
| `/flywheel-health` | Check learning flywheel health — scores each stage, identifies broken links, suggests fixes |
| `/handoff` | Hand off a task to another pane with structured context and notification |
| `/harness-audit` | Run a deterministic repository harness audit and return a prioritized scorecard |
| `/hotfix` | Emergency fix workflow for production issues with hotfix branch and deploy |
| `/instinct-export` | Export instincts from project/global scope to a file |
| `/instinct-graduate` | Review high-confidence instincts and graduate them to skills or rules |
| `/instinct-import` | Import instincts from file or URL into project/global scope |
| `/instinct-status` | Show learned instincts (project + global) with confidence |
| `/learn-eval` | "Extract reusable patterns from the session, self-evaluate quality before saving, and determine the right save location  |
| `/migrate` | Supabase migration workflow — generate, validate, and apply schema changes |
| `/model-route` | Recommend the best model tier for the current task by complexity and budget |
| `/morning` | — |
| `/projects` | List known projects and their instinct statistics |
| `/promote` | Promote project-scoped instincts to global scope |
| `/prompt-optimize` | Analyze a draft prompt and output an optimized, ECC-enriched version ready to paste and run. Does NOT execute the task � |
| `/prune` | Delete pending instincts older than 30 days that were never promoted |
| `/quality-gate` | Run the ECC quality pipeline on demand for a file or project scope |
| `/refactor-clean` | Safely identify and remove dead code with test verification at every step |
| `/resume-session` | Load the most recent session file from ~/.claude/sessions/ and resume work with full context from where the last session |
| `/rules-distill` | "Scan skills to extract cross-cutting principles and distill them into rules" |
| `/save-session` | Save current session state to a dated file in ~/.claude/sessions/ so work can be resumed in a future session with full c |
| `/sessions` | Manage Claude Code session history, aliases, and session metadata. |
| `/skill-create` | Analyze local git history to extract coding patterns and generate SKILL.md files. Local version of the Skill Creator Git |
| `/skill-health` | Show skill portfolio health dashboard with charts and analytics |
| `/test-coverage` | Analyze test coverage, identify gaps, and generate missing tests to reach 80%+ |
| `/update-codemaps` | Analyze codebase structure and generate token-lean architecture documentation |
| `/update-docs` | Sync documentation with the codebase, generating from source-of-truth files |
| `/verify` | Run comprehensive verification on current codebase (build, lint, types, tests) |
| `/weekly` | — |
